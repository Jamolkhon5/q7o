-- Откат очистки запросов контактов

-- Удаляем индекс если он был создан
DROP INDEX IF EXISTS idx_contact_requests_pending;

-- Нельзя восстановить удаленные записи, но можем пересоздать таблицу
-- В продакшене лучше делать backup перед такими миграциями